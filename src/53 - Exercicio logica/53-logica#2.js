/**
 * Escrever uma função que receba dois argumentos, largura e altura
 * de uma imagem e retorne true se a imagem estiver em modo paisagem
 */
const image = (width, height) => width > height;

console.log(image(120, 500));
